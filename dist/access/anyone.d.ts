import type { Access } from 'payload';
/**
 * Access control that allows anyone to read or write
 * @returns True if the user is authenticated, false otherwise
 */
export declare const anyone: Access;
